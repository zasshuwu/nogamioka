import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
	palette: {
		primary: {
			main: "#30d95d",
		},
		secondary: {
			main: "#d9305a",
		},
		navButton: "#f53652",
		background: {
			main: "#141414",
		},
	},
});

export default theme;
